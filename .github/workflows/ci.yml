name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features

      - name: Unused deps
        run: |
          cargo install cargo-machete --locked
          cargo machete

  test:
    name: Test
    needs: lint
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run tests
        run: cargo test --workspace --all-features

  build:
    name: Build
    needs: lint
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build release
        run: cargo build --workspace --release

  # FR-001 phase-06: enforce 95% line coverage on the gateway files
  # delivered by phases 01-05. Files outside that scope (cache, lb, ssl,
  # tunnel, http3, plus proxy/router glue that needs the deferred
  # integration harness) are excluded so we measure only the code under
  # this phase's contract.
#  coverage:
#    name: Gateway coverage (>=95%)
#    needs: lint
#    runs-on: ubuntu-latest
#    timeout-minutes: 30
#    steps:
#      - uses: actions/checkout@v4
#      - uses: dtolnay/rust-toolchain@stable
#        with:
#          components: llvm-tools-preview
#      - uses: Swatinem/rust-cache@v2
#      - name: Install cargo-llvm-cov
#        uses: taiki-e/install-action@cargo-llvm-cov
#      - name: Run llvm-cov
#        run: |
#          cargo llvm-cov -p gateway --lcov --output-path lcov.info \
#            --ignore-filename-regex '(cache|lb|tunnel|ssl|http3|proxy|proxy_waf_response|context|router|lib|request_ctx_builder|protocol)\.rs$|/tests/|/benches/' \
#            | tee cov-summary.txt
#      - name: Enforce 95% line coverage gate
#        # cargo-llvm-cov 0.8.5 ignores --fail-under-lines when combined with
#        # --ignore-filename-regex, so we parse the TOTAL line ourselves.
#        run: |
#          pct=$(awk '/^TOTAL/ { gsub("%","",$10); print $10 }' cov-summary.txt)
#          echo "gateway scoped line coverage: ${pct}%"
#          awk -v p="$pct" 'BEGIN { exit (p+0 < 95.0) }' \
#            || { echo "::error::Coverage ${pct}% below 95% gate"; exit 1; }
#      - name: Upload lcov artifact
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: gateway-lcov
#          path: lcov.info
